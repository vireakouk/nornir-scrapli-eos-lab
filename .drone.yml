kind: pipeline
type: docker
name: netdevops cicd

steps:
  - name: starting build
    image: vireakouk/netdevops-drone-nornir-scrapli:1.0
    commands:
      - echo 'starting build'
      - echo 'hello world'
      - git fetch
      - git checkout ${DRONE_BRANCH}
  
  - name: dry run
    image: vireakouk/netdevops-drone-nornir-scrapli:1.0
    commands:
      - echo 'dry run'
      - git --version
    when:
      branch:
        - dev
  
  # - name: merge to main 
  #   image: vireakouk/netdevops-drone-nornir-scrapli
  #   commands:
  #     - git checkout main
  #     - git branch -u origin/main
  #     - git merge dev
  #     - git push http://$GITLAB_USER:$GITLAB_PASS:192.168.1.200/vireakouk/${DRONE_REPO}
  #   secrets: [ GITLAB_USER, GITLAB_PASS ]
  #   when:
  #     branch:
  #       - dev
        
  # - name: deploy to production
  #   image: vireakouk/netdevops-drone-nornir-scrapli
  #   commands:
  #     - python deploy_baseconfig.py
  #   when:
  #     branch:
  #       - main

  # - name: send telegram notification
  #   image: appleboy/drone-telegram
  #   settings:
  #     token: xxxxxxxxxx
  #     to: telegram_user_id
  #     message: >
  #       {{#success build.status}}
  #         build {{build.number}} succeeded. Good job.
  #       {{else}}
  #         build {{build.number}} failed. Fix me please.
  #       {{/success}}