kind: pipeline
type: docker
name: netdevops cicd

steps:
  - name: starting build
    image: vireakouk/netdevops-drone-nornir-scrapli:latest
    commands:
      - echo 'starting build'
      - git --version
      - python --version
      - pip list
      - git fetch
      - git checkout ${DRONE_BRANCH}
  
  - name: dry run
    image: ubuntu:20.04
    environment:
      NORNIR_USER:
        from_secret: NORNIR_USER
      NORNIR_SECRET:
        from_secret: NORNIR_SECRET
    commands:
      - ip route add 172.100.1.0/24 via 172.17.0.1
      - ip route
      - pip install nornir nornir-jinja2 nornir-scrapli nornir-utils
      - python deploy_baseconfig.py --dry_run
    when:
      branch:
        - dev
  
  # - name: merge to main 
  #   image: vireakouk/netdevops-drone-nornir-scrapli:latest
  #   commands:
  #     - git checkout main
  #     - git branch -u origin/main
  #     - git merge dev
  #     - git push http://$GITLAB_USER:$GITLAB_PASS:192.168.1.200/vireakouk/${DRONE_REPO}
  #   secrets: [ GITLAB_USER, GITLAB_PASS ]
  #   when:
  #     branch:
  #       - dev
        
  # - name: deploy to production
  #   image: vireakouk/netdevops-drone-nornir-scrapli
  #   commands:
  #     - python deploy_baseconfig.py
  #   when:
  #     branch:
  #       - main

  # - name: send telegram notification
  #   image: appleboy/drone-telegram
  #   settings:
  #     token: xxxxxxxxxx
  #     to: telegram_user_id
  #     message: >
  #       {{#success build.status}}
  #         build {{build.number}} succeeded. Good job.
  #       {{else}}
  #         build {{build.number}} failed. Fix me please.
  #       {{/success}}